<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="BuySell.CustomControl.ListEmptyStateView">
    <ContentView.Content>
         <Grid>
            <ActivityIndicator
                Grid.Row="0"
                x:Name="spinner"
                VerticalOptions="Center"
                IsVisible="true"
                IsRunning="true"/>
            <ContentView
                Grid.Row="0"
                x:Name="emptyState"
                IsVisible="false"
                VerticalOptions="Center"
                HorizontalOptions="Center" />
        </Grid>
    </ContentView.Content>
</ContentView>

